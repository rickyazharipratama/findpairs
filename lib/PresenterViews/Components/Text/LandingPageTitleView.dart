import 'package:flutter/widgets.dart';

class LandingPageTitleView{

  TickerProvider _tick;

  TickerProvider get tick => _tick;
  set setTick(TickerProvider tck){
    _tick = tck;
  }

  notifyState(){}

}